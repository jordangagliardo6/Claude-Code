{
  "name": "lead-gen-workflow",
  "version": "1.0.0",
  "description": "Automated lead generation for HVAC companies in Southwest Michigan",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "verify": "node verify.js",
    "run-now": "node -e \"require('dotenv').config(); require('./src/workflow').runWorkflow().then(() => process.exit(0));\""
  },
  "dependencies": {
    "axios": "^1.6.0",
    "dotenv": "^16.3.1",
    "googleapis": "^128.0.0",
    "node-cron": "^3.0.3",
    "winston": "^3.11.0"
  }
}
