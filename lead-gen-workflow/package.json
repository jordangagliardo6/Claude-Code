{
  "name": "lead-gen-workflow",
  "version": "1.0.0",
  "description": "Automated HVAC lead generation for Southwest Michigan using Apollo.io and Google Sheets",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "setup": "node setup.js",
    "run-now": "node -e \"require('./src/workflow').runWorkflow().then(() => process.exit(0)).catch(e => { console.error(e); process.exit(1); })\""
  },
  "dependencies": {
    "axios": "^1.7.2",
    "dotenv": "^16.4.5",
    "googleapis": "^140.0.1",
    "node-cron": "^3.0.3",
    "nodemailer": "^6.9.14"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
