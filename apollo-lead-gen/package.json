{
  "name": "apollo-lead-gen",
  "version": "1.0.0",
  "description": "Automated HVAC lead generation for SW Michigan — Apollo.io → Google Sheets, runs daily at 7am ET",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "node index.js --test",
    "auth": "node index.js --auth",
    "verify": "node index.js --verify"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "googleapis": "^144.0.0",
    "node-cron": "^3.0.3",
    "node-fetch": "^3.3.2",
    "nodemailer": "^6.9.14",
    "open": "^10.1.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
