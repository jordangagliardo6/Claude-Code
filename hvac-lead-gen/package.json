{
  "name": "hvac-lead-gen",
  "version": "1.0.0",
  "description": "Automated HVAC lead generation for Southwest Michigan via Apollo.io → Google Sheets",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "setup-check": "node setup-check.js",
    "run-once": "node -e \"require('dotenv').config(); require('./workflow').runWorkflow()\""
  },
  "dependencies": {
    "axios": "^1.7.2",
    "dotenv": "^16.4.5",
    "googleapis": "^140.0.1",
    "node-cron": "^3.0.3"
  }
}
